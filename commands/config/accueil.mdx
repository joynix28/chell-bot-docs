---
title: '/config-accueil'
description: 'Configurer les messages de bienvenue et d\'au revoir'
icon: 'door-open'
---

## Description

Créez des messages personnalisés pour accueillir les nouveaux membres et dire au revoir à ceux qui partent.

## Utilisation

```bash
/config-accueil
```

Un menu interactif s'affiche pour configurer tous les paramètres.

## Permissions requises

<Warning>
  **Vous :** Permission `Gérer le serveur`  
  **Bot :** Permissions `Envoyer des messages`, `Intégrer des liens`, `Attacher des fichiers` dans le salon choisi
</Warning>

## Configuration pas à pas

<Steps>
  <Step title="Activer le module">
    Cliquez sur 👋 **Activer l'accueil**
  </Step>
  
  <Step title="Choisir le salon">
    Sélectionnez le salon où envoyer les messages (ex: `#bienvenue`)
  </Step>
  
  <Step title="Personnaliser le message">
    Utilisez les variables disponibles pour créer votre message
  </Step>
  
  <Step title="Tester">
    Utilisez `/test-accueil` pour prévisualiser
  </Step>
</Steps>

## Variables disponibles

<ResponseField name="{user}" type="mention">
  Mention du membre (@Pseudo)
</ResponseField>

<ResponseField name="{user.name}" type="texte">
  Nom d'utilisateur (sans mention)
</ResponseField>

<ResponseField name="{user.id}" type="nombre">
  ID Discord du membre
</ResponseField>

<ResponseField name="{user.tag}" type="texte">
  Tag complet (Pseudo#0000)
</ResponseField>

<ResponseField name="{server}" type="texte">
  Nom du serveur
</ResponseField>

<ResponseField name="{server.count}" type="nombre">
  Nombre total de membres
</ResponseField>

<ResponseField name="{server.count.ordinal}" type="texte">
  Nombre ordinal (1er, 2ème, 3ème...)
</ResponseField>

<ResponseField name="{date}" type="texte">
  Date actuelle
</ResponseField>

<ResponseField name="{time}" type="texte">
  Heure actuelle
</ResponseField>

## Exemples de messages

### Message simple
```txt
Bienvenue {user} sur **{server}** !
Tu es le {server.count.ordinal} membre ! 🎉
```

### Message avec règles
```txt
👋 Bienvenue {user} !

Nous sommes ravis de t'accueillir sur **{server}** !

📜 Pense à lire notre <#ID_SALON_REGLES>
🎭 Récupère tes rôles dans <#ID_SALON_ROLES>
💬 Présente-toi dans <#ID_SALON_PRESENTATIONS>

Bon amusement parmi nous ! 🎉
Tu es notre {server.count.ordinal} membre !
```

### Message d'au revoir
```txt
👋 {user.name} vient de quitter **{server}**...
Nous sommes maintenant {server.count} membres.

Bonne continuation ! ❤️
```

## Embed personnalisé

Pour un message encore plus esthétique, utilisez un embed :

<Steps>
  <Step title="Activer le mode embed">
    Dans le menu, cliquez sur **🎨 Mode Embed**
  </Step>
  
  <Step title="Personnaliser">
    - **Titre** : "Bienvenue !"
    - **Description** : Votre message avec variables
    - **Couleur** : Choisissez une couleur (hex)
    - **Image** : URL d'une bannière
    - **Thumbnail** : Avatar du membre (`{user.avatar}`)
    - **Footer** : "Membre #{server.count}"
  </Step>
</Steps>

## Image de bienvenue personnalisée

Générez automatiquement une image de bienvenue :

```bash
/config-accueil image:Activer template:[numéro]
```

**Templates disponibles :**
1. Template moderne bleu
2. Template élégant noir
3. Template coloré dégradé
4. Template minimaliste

L'image contient :
- Avatar du membre
- Pseudo
- Nombre de membres
- Date de création du compte

## Message d'au revoir

Pour configurer le message de départ :

```bash
/config-accueil type:depart
```

Les mêmes variables sont disponibles.

## Options avancées

### Supprimer automatiquement
```bash
/config-accueil auto-delete:5m
```
Le message de bienvenue sera supprimé après 5 minutes.

### Attribution auto-rôle
```bash
/config-accueil role:@Membre
```
Attribue automatiquement un rôle à l'arrivée.

### Salon multiple
```bash
/config-accueil salon-secondaire:#logs-join
```
Envoi aussi dans un salon de logs.

## Test de configuration

```bash
/test-accueil
```

Cela simule votre arrivée sur le serveur.

## Désactiver

```bash
/config-accueil desactiver
```

## Voir aussi

- [/config-mp-onjoin](/commands/config/mp-onjoin) - MP personnalisé à l'arrivée
- [/config-autoroles](/commands/config/autoroles) - Rôles automatiques
- [/config-verif](/commands/config/verif) - Vérification avant accès
