---
title: '/clear'
description: 'Supprimer des messages en masse'
icon: 'eraser'
---

## Description

Permet de supprimer plusieurs messages d'un salon en une seule commande. Utile pour nettoyer du spam ou des conversations inappropriées.

## Utilisation

```bash
/clear quantité: [nombre] membre: [@utilisateur] contient: [texte]
```

## Paramètres

<ParamField path="quantité" type="nombre" required>
  Nombre de messages à supprimer (1-100)
</ParamField>

<ParamField path="membre" type="utilisateur">
  Supprimer uniquement les messages de cet utilisateur
</ParamField>

<ParamField path="contient" type="texte">
  Supprimer uniquement les messages contenant ce texte
</ParamField>

<ParamField path="bots" type="booléen">
  Supprimer uniquement les messages des bots
</ParamField>

## Permissions requises

<Warning>
  **Vous :** Permission `Gérer les messages`  
  **Bot :** Permission `Gérer les messages`
</Warning>

## Limitation Discord

<Info>
Discord ne permet de supprimer que les messages de **moins de 14 jours**. Les messages plus anciens doivent être supprimés manuellement.
</Info>

## Exemples d'utilisation

<CodeGroup>
```bash Supprimer 50 messages
/clear quantité:50
```

```bash Messages d'un membre spécifique
/clear quantité:100 membre:@Spammeur
```

```bash Messages contenant un mot
/clear quantité:50 contient:spam
```

```bash Messages des bots uniquement
/clear quantité:30 bots:Oui
```
</CodeGroup>

## Messages de confirmation

Après suppression, le bot affiche un récapitulatif :
- ✅ Nombre de messages supprimés
- 👤 Utilisateur ciblé (si spécifié)
- 📝 Filtre appliqué (si utilisé)

Ce message s'auto-supprime après 5 secondes.

## Logs automatiques

Si les logs de modération sont configurés, chaque utilisation de `/clear` est enregistrée avec :
- Modérateur responsable
- Nombre de messages supprimés
- Salon concerné
- Filtres appliqués

## Bonnes pratiques

<Warning>
- Ne supprimez pas de messages contenant des **preuves d'infraction**
- Faites des captures d'écran si nécessaire **avant** de clear
- Utilisez les filtres pour éviter de supprimer des messages importants
</Warning>

## Voir aussi

- [/setup-logs](/commands/config/logs) - Configurer les logs
- [/purge](/commands/moderation/purge) - Alternative avec plus d'options
